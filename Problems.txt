
= Problems =
1. Method `read_sequence_log` from `ExperimentRunner.py` requires a globalLog.csv file to be generated but, it seems that the file is only read but never written, thus resolving into a read/file-not-found error.